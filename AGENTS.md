# 项目协作说明

- 文档和面向用户的说明用中文。先读 README.md 与 skills/science-video-production/SKILL.md。
- 保留两个已交付实例的口播、配色和时间轴，除非用户要求修改。
- 生成音频与渲染视频是独立操作。不要因为渲染缺少音轨就调用计费接口。
- 认证只从环境读取；不要输出秘密值。local_media、runs、outputs 不提交；media 中仅允许已批准的案例文件。
- 新音轨重新对齐；转写检查不等于人工听音。不要伪造模型实测或性能数据。
- 提交前运行暂存快照检查与相关测试。不要用强推覆盖远端历史。
